# R publication validation

These scripts use repository-relative source CSVs and do not install packages. They gracefully report unavailable optional packages. Run `Rscript validate_publication_data.R` before `Rscript make_publication_figures.R`.
