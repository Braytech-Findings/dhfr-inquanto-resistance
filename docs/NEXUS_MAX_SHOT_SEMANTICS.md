# Nexus Maximum-Shot Semantics

OBJECTIVE COMPUTATIONAL OUTPUT — RESEARCHER INTERPRETATION REQUIRED

The installed qnexus signature accepts either one integer or a list of per-item
shot counts. The previous four-circuit pilot passed `[10, 10, 10, 10]` and each
returned result contained exactly ten shots. This proves per-circuit list
semantics at ten shots.

The requested staged 100, 1,000, and 10,000-shot experiments were not run.
The production plan requires successful InQuanto checkpoint loading and a
validated complete molecular estimator before Phase E. License initialization
blocked checkpoint loading in Phase B. Running high-shot toy circuits would not
advance the molecular study and would violate the stated phase order.

Maximum accepted shots remain `UNVERIFIED`. Maximum stable batch size remains
four circuits—the largest previously completed test, not an asserted provider
limit.

