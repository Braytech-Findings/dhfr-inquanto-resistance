# Inspection notes

Protein Viewer was not programmatically operated in this workflow. Reproducible PDB/XYZ render scripts and selection metadata are the authoritative structural record. Inspect `data/processed/WT_TMP_minimized.pdb` with a local viewer using chain `A` for protein and residue `TOP` on chain `X` for trimethoprim.
