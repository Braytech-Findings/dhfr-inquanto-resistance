# Molecular visualization

Run `python scripts/render_molecular_3d.py` to create repository-relative molecular figures. These are programmatic renderings of the prepared WT_TMP PDB and QM-cluster XYZ, not manually edited screenshots.
